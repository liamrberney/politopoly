package com.berneytech.politopoly;

public class RectGamePiece implements IGamePiece {
    static int maxMoves = 10;
    int moves;
    int x;
    int y;
    RectGamePiece(){
        x=0; y=0;
        moves = 0;
    }
    public void move(int a){
        moves++;
        if moves % maxMoves == 0
                if moves / maxMoves == 0 then
                    x += 0
                    y += 2 * step
                if moves / maxMoves == 1 then
        if moves / maxMoves == 0
        else if moves / maxMoves == 1
    }
}
